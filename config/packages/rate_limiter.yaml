framework:
    rate_limiter:

        admin_login:
            policy: fixed_window
            limit: 5
            interval: '15 minutes'

        captcha:
            policy: fixed_window
            limit: 10
            interval: '5 minutes'

        otp:
            policy: fixed_window
            limit: 5
            interval: '10 minutes'

        allowed_ips:
            policy: fixed_window
            limit: 100
            interval: '1 minute'